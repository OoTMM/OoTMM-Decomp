{
  "name": "@ootmm/game",
  "scripts": {
    "build": "make"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@ootmm/data": "workspace:*"
  },
  "nx": {
    "targets": {
      "build": {
        "inputs": [
          "{projectRoot}/src/**/*",
          "{projectRoot}/include/**/*"
        ],
        "outputs": [
          "{projectRoot}/dist/**/*"
        ]
      }
    }
  }
}
